_build/
_opam/
.fennec/
*.install
*.merlin
.DS_Store

# `fennec release` stages the deployable binary here (also `fennec build`'s default outdir)
dist/

# npm: examples install node deps locally; lockfiles are committed for
# reproducible CI installs, node_modules is not.
node_modules/

# native build outputs (regenerated per platform by the dune rule)
**/libfennec_esbuild.a
**/libfennec_esbuild.h
**/libfennec_css.a
**/link_flags.sexp
**/native/rust/target/
