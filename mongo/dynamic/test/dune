; The transparent transaction context (Fennec_mongo_dynamic.Tx): over the in-memory backend (test_tx,
; outside an Eio scheduler — the global-fallback arm) and over the REAL burrow LMDB backend
; (test_tx_burrow, under an Eio scheduler — the native-rollback path).
(tests
 (names test_tx test_tx_burrow test_authz)
 (libraries fennec-mongo.dynamic fennec-mongo.bson fennec-mongo.minimongo fennec-mongo.backend eio_main eio
   unix))
