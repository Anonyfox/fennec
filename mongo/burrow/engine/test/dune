; Engine test suites: the read pipeline (test_engine), the mutation path (test_writes), and the
; secondary-index access paths incl. range/multikey/unique/backfill/persistence (test_index) — all
; through the real storage stack under an Eio scheduler.
(tests
 (names test_engine test_writes test_index)
 (libraries burrow burrow_store eio_main unix fennec-mongo.bson))
