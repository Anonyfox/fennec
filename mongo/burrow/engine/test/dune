; Engine test suites: the read pipeline (test_engine), the mutation path (test_writes), the
; secondary-index access paths incl. range/multikey/unique/backfill/persistence (test_index), and the
; online hot backup (test_backup) — all through the real storage stack under an Eio scheduler.
(tests
 (names test_engine test_writes test_index test_diff test_multikey test_observe test_concurrent test_blob
   test_txn test_backup test_usage test_governance test_held test_online_index)
 (libraries burrow burrow_store eio_main unix fennec-mongo.bson fennec-mongo.minimongo))
