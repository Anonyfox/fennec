; Lifecycle test against a REAL mongod when one is installed (it launches an ephemeral instance,
; connects, and stops it); it skips — and passes — where no mongod is present, so CI without MongoDB
; stays green.
(test
 (name test_mongod)
 (libraries fennec-mongo.mongod unix fennec-hunt.unit)
 (preprocess
  (pps fennec-hunt.ppx)))
