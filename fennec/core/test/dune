; Colocated unit tests for the pure core modules. Each module gets its own test
; executable so a failure points straight at the module. All run on `dune test`.
(tests
 (names test_http test_http_semantics test_http_date test_mime test_app)
 (modes native)
 (libraries fennec.core routes))
