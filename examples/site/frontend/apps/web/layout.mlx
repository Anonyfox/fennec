(* the web app's persistent shell: the shared nav + the routed outlet.
   Links use TYPED paths: `p "/about"` is the in-app path (router-managed, base-aware,
   intercepted for SPA nav), `ext "https://…"` is an outward raw URL (left to the
   browser). Both are compile-checked format strings. *)
let view =
  <div id="app-root">
    <Nav links=[
      (p "/", "Home");
      (p "/about", "About");
      (p "/pricing", "Pricing");
      (p "/products", "Products");
      (ext "https://github.com/Anonyfox/fennec", "GitHub ↗");
    ] />
    (* the anonymous→personalized "snap": renders "Sign in" in the SSR (cacheable) frame, then
       fills in "Hello, …" after hydration once the session re-scopes. See User_badge / the README
       section "SSR + the reactive user". *)
    <User_badge />
    (outlet ())
  </div>
